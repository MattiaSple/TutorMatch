<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>
        <variable
            name="chat"
            type="com.example.tutormatch.data.model.Chat" />
    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="8dp">

        <!-- Layout orizzontale per nome partecipante e soggetto -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal">

            <!-- Visualizza il nome del partecipante selezionato -->
            <TextView
                android:id="@+id/textViewSelectedParticipant"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="@{chat.participantsNames.size() > 0 ? chat.participantsNames[0] : `Partecipante non disponibile`}"
                android:textSize="14sp"
                android:textColor="@android:color/black"
                android:ellipsize="end"
                android:maxLines="1" />

            <!-- Visualizza il soggetto della chat -->
            <TextView
                android:id="@+id/textViewSubject"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text='@{chat.subject}'
                android:textSize="14sp"
                android:textColor="@android:color/black"
                android:ellipsize="end"
                android:maxLines="1"
                android:layout_gravity="end"/>
        </LinearLayout>

        <!-- Visualizza l'ultimo messaggio -->
        <TextView
            android:id="@+id/textViewLastMessage"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text='@{chat.lastMessage != null ? chat.lastMessage.text : "Nessun messaggio"}'
            android:textSize="16sp"
            android:textColor="@android:color/black"
            android:ellipsize="end"
            android:maxLines="1"/>
    </LinearLayout>
</layout>
